let arr = [1, 2, 3, 4, 5, 6];
// slice(start, end) extracts elements from the 'start' index 
// up to (but NOT including) the 'end' index. 
// It returns a new array and does not change the original one.
let newArr = arr.slice(1, 3);
console.log(arr);
console.log(newArr);
