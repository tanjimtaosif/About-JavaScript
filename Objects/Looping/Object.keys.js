let obj = {
    name: "Tanjim",
    age: 23,
    email: "test@test.com",
};

/**
 * HOW TO USE:
 * Object.keys(obj) returns an array of strings representing the object's keys.
 * Output: ["name", "age", "email"]
 */
console.log(Object.keys(obj));