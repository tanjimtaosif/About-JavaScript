// ?:

// Condition ? If the condition is ture  : If not then this
// 12 > 15 ? console.log("true") : console.log("false");

let points = 20;
let status = points > 100 ? "Gold" : points > 50 ? "Silver" : "Bronze";

console.log(status);


